# 配置驱动型后台原型框架 - 实现计划（分解和优先级排序）

## [ ] 任务 1：初始化项目环境
- **优先级**：P0
- **依赖**：None
- **描述**：
  - 使用 Vite 创建 React 18 + TypeScript 项目
  - 安装 Ant Design 5 和 React Router 6 依赖
  - 配置基础项目结构
- **验收标准**：AC-1, AC-6
- **测试要求**：
  - `programmatic` TR-1.1：项目能成功构建，无TypeScript错误
  - `human-judgment` TR-1.2：项目结构清晰，依赖安装正确
- **注意**：确保使用最新版本的依赖包

## [ ] 任务 2：创建类型定义文件
- **优先级**：P0
- **依赖**：任务 1
- **描述**：
  - 创建 `src/types/list-config.d.ts` 文件
  - 定义 JSON 配置文件的类型接口
  - 确保类型定义完整覆盖所有配置字段
- **验收标准**：AC-6
- **测试要求**：
  - `programmatic` TR-2.1：TypeScript 类型检查通过
  - `human-judgment` TR-2.2：类型定义清晰，符合 JSON 配置结构
- **注意**：类型定义应严格按照用户提供的 JSON 配置结构

## [ ] 任务 3：实现列类型映射引擎
- **优先级**：P0
- **依赖**：任务 2
- **描述**：
  - 创建 `src/utils/columnMapper.tsx` 文件
  - 实现不同类型列的渲染逻辑
  - 支持 `text`、`link`、`currency`、`datetime`、`tag`、`actions` 类型
- **验收标准**：AC-2, AC-3
- **测试要求**：
  - `human-judgment` TR-3.1：每种列类型都能正确渲染
  - `human-judgment` TR-3.2：标签渲染能正确读取颜色和样式配置
- **注意**：货币类型需要自动添加千分位和两位小数，日期时间需要格式化

## [ ] 任务 4：实现通用列表组件
- **优先级**：P0
- **依赖**：任务 3
- **描述**：
  - 创建 `src/components/GenericListPage.tsx` 文件
  - 实现 JSON 配置解析逻辑
  - 实现搜索区、数据表格、分页器、操作栏的渲染
  - 实现前端内存级分页、搜索筛选和删除模拟功能
- **验收标准**：AC-1, AC-4, AC-5
- **测试要求**：
  - `human-judgment` TR-4.1：页面能正确渲染所有组件
  - `human-judgment` TR-4.2：搜索、分页、删除操作能正确执行
  - `human-judgment` TR-4.3：修改 JSON 配置后页面能自动刷新
- **注意**：使用 setTimeout 模拟网络延迟，确保交互体验流畅

## [ ] 任务 5：配置路由
- **优先级**：P1
- **依赖**：任务 4
- **描述**：
  - 创建 `src/router/index.tsx` 文件
  - 配置集中式路由表
  - 示例演示如何挂载 `/orders` 并传入 JSON 路径
- **验收标准**：AC-1
- **测试要求**：
  - `human-judgment` TR-5.1：路由配置正确，能访问到列表页
  - `human-judgment` TR-5.2：JSON 配置路径能正确传递给组件
- **注意**：确保路由配置符合 React Router 6 的语法

## [ ] 任务 6：创建示例配置文件
- **优先级**：P1
- **依赖**：任务 4
- **描述**：
  - 创建 `src/mock/order.config.json` 文件
  - 提供 8 条真实感数据
  - 包含 2 种状态字典
  - 配置完整的搜索、列和操作
- **验收标准**：AC-1, AC-2, AC-3
- **测试要求**：
  - `human-judgment` TR-6.1：配置文件结构正确
  - `human-judgment` TR-6.2：数据能正确渲染
- **注意**：数据应具有真实感，覆盖不同的列类型

## [ ] 任务 7：创建主应用入口
- **优先级**：P1
- **依赖**：任务 5
- **描述**：
  - 更新 `src/App.tsx` 文件
  - 配置路由和全局布局
  - 确保应用能正常启动
- **验收标准**：AC-1
- **测试要求**：
  - `human-judgment` TR-7.1：应用能正常启动
  - `human-judgment` TR-7.2：页面布局符合 Ant Design 规范
- **注意**：添加基本的布局结构，确保页面美观

## [ ] 任务 8：创建 PM 零代码使用指南
- **优先级**：P2
- **依赖**：任务 6
- **描述**：
  - 创建使用指南文档
  - 说明如何新增业务页、如何替换 JSON、热更新验证步骤
  - 提供字段类型速查表
- **验收标准**：AC-1, AC-5
- **测试要求**：
  - `human-judgment` TR-8.1：文档内容清晰，易于理解
  - `human-judgment` TR-8.2：步骤说明详细，可操作性强
- **注意**：文档应面向非技术人员，使用简单易懂的语言